<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.user.UserDashboardActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#FFFFFF">

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:elevation="0dp"
            android:background="@android:color/transparent">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:title="MediAssist"
                app:titleTextColor="#333333" />
        </com.google.android.material.appbar.AppBarLayout>

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingHorizontal="24dp"
            android:paddingTop="8dp"
            android:paddingBottom="24dp">

            <TextView
                android:id="@+id/tvSubtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Your personal health hub"
                android:textColor="#888888"
                android:textSize="14sp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/txtProfileStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:paddingVertical="16dp"
                android:text="Profile completed"
                android:textAlignment="center"
                android:textColor="#4CAF50"
                android:textSize="14sp"
                android:textStyle="bold"
                app:layout_constraintTop_toBottomOf="@id/tvSubtitle" />

            <!-- ROW: PROFILE + CONTACTS -->
            <LinearLayout
                android:id="@+id/rowButtons"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:orientation="horizontal"
                android:weightSum="2"
                app:layout_constraintTop_toBottomOf="@id/txtProfileStatus">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnProfile"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="120dp"
                    android:layout_marginEnd="8dp"
                    android:layout_weight="1"
                    android:text="My Profile"
                    android:textColor="#333333"
                    android:textSize="12sp"
                    app:backgroundTint="#FFFFFF"
                    app:cornerRadius="16dp"
                    app:icon="@android:drawable/ic_menu_my_calendar"
                    app:iconGravity="top"
                    app:iconPadding="12dp"
                    app:iconTint="#A0A0A0"
                    app:strokeColor="#E0E0E0" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnContacts"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="120dp"
                    android:layout_marginStart="8dp"
                    android:layout_weight="1"
                    android:text="Contacts"
                    android:textColor="#333333"
                    android:textSize="12sp"
                    app:backgroundTint="#FFFFFF"
                    app:cornerRadius="16dp"
                    app:icon="@android:drawable/ic_menu_call"
                    app:iconGravity="top"
                    app:iconPadding="12dp"
                    app:iconTint="#A0A0A0"
                    app:strokeColor="#E0E0E0" />
            </LinearLayout>

            <!-- NEW: BROWSE MEDICINES CARD (Same Style) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnBrowseMedicines"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:layout_marginTop="24dp"
                android:text="Browse Medicines"
                android:textColor="#333333"
                android:textSize="12sp"
                app:backgroundTint="#FFFFFF"
                app:cornerRadius="16dp"
                app:icon="@android:drawable/ic_menu_search"
                app:iconGravity="top"
                app:iconPadding="12dp"
                app:iconTint="#A0A0A0"
                app:strokeColor="#E0E0E0"
                app:layout_constraintTop_toBottomOf="@id/rowButtons" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/cardMedicalAssistant"
                android:layout_width="match_parent"
                android:layout_height="65dp"
                android:layout_marginTop="32dp"
                android:text="AI Assistant - Ask health questions"
                android:textAllCaps="false"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                app:backgroundTint="#121212"
                app:cornerRadius="16dp"
                app:layout_constraintTop_toBottomOf="@id/btnBrowseMedicines" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnEmergency"
                android:layout_width="match_parent"
                android:layout_height="65dp"
                android:layout_marginTop="24dp"
                android:text="REQUEST AMBULANCE"
                android:textColor="#FFFFFF"
                android:textStyle="bold"
                app:backgroundTint="#2196F3"
                app:cornerRadius="32dp"
                app:layout_constraintTop_toBottomOf="@id/cardMedicalAssistant" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:menu="@menu/user_drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>